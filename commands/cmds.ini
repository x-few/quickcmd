[check-listen-port]
command = netstat -aLn | grep ${port}

[tcpdump-port]
command = sudo tcpdump -i ${interface} port ${port} -v

[tcpdump-write-file]
command = sudo tcpdump -i ${interface} port ${port} -v -w ${filename}

