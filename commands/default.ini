[go-home]
godir = ~/

[go-quickcmd]
godir = ~/.quickcmd

[tip-merge-to-master]
tip = git pull origin ${your_branch}
	git rebase -i HEAD~${commit_num}
	git push origin $[your_branch}
	git pull --rebase origin master
	git checkout master
	git merge ${your_branch}
	git push origin master

